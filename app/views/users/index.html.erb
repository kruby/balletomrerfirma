<%= render :partial => "layouts/menu" %>
<div id="admin_liste">
<h1>Brugerliste <%= link_to '[Ny bruger]', new_user_path %></h1>

<table id='zebra_stripes'>
  <tr>
	<th>ID</th>
	<th>Aktiv</th>
	<th>Kategori</th>
    <th>Login</th>
    <th>e-mail</th>
	<th>Blogmail</th>
	<th></th>
  </tr>

<% for user in @users %>
  <tr>
	<td><%=h user.id %></td>
	<td class='pos'><%=link_to active_test(user.active), :action => 'active_users', :id => user.id%></td>
	<td class="pos"><%=link_to user.category, :action => 'category', :id => user.id rescue nil%></td> <%# access findes i application.rb %>	
    <td width=100px><%=h user.login %></td>
    <td width=200px><%=h user.email %></td>
	<td class="pos"><%=link_to active_test(user.blogmail), :action => 'blogmail', :id => user.id%></td>
	<!-- <td class="pos"><%#=h user.blogname %></td> -->

	<td>
	<%= link_to '[Ret]', edit_user_path(user) %>
    <%= link_to '[Slet]', user, :confirm => "Er du sikker på du vil slette denne bruger: '#{user.login}'?", :method => :delete %>
	</td>
  </tr>

<% end %>

</table>

<br />


</div>

<%= render :partial => "layouts/zebra_stripes" %>