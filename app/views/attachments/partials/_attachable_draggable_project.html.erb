<div id="attachments_links">
	<% @page %>
	<%= link_to '[Tilbage]', edit_project_path(params[:project_id]) %>
	<%= link_to '[Opdater]', projects_attachments_paged_path(params[:project_id]) %><br>
</div>

<%= render :partial => '/attachments/partials/attachments'%>


<% content_for :javascript do %>

  <% javascript_tag do %>

     		$('.draggable').draggable();
		    $("#receive_images").droppable({
		      drop: function(project, ui){
			        $.ajax({
		              type: 'post', 
		              data: 'id=' + $(ui.draggable).attr('id') + '&project_id=<%=@attachable.id-%>', 
		              dataType: 'script',
		              url: '/attachments/create'})
		            }
		    });
		



    <% end %>

<% end %>

