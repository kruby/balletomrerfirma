<div id="forside_events">
	
	<% for event in @events %>

		<% if event.attachments.count > 0 %>
	
		

					<div id="event">

						<% @attachment = event.attachments.first %>
			
							<%# @alt = Asset.find(	attachment.asset_id).description rescue nil %>
							<% @alt = @attachment.description rescue nil %>
							<% @image = Asset.find(@attachment.asset_id).photo.url(:image_200) %>
							<% @image_show = Asset.find(@attachment.asset_id).photo.url(:original)%>
					
							<div id="clip_outer">
								<div id="clip_inner">
									<span class='thumb' id='event_images_<%=event.id%>'>
										<%= link_to image_tag(@image, :alt => @alt), event %><%#= link_to image_tag(@image, :alt => @alt), @image_show %>
									</span>
									<!-- HER HAR VI BRUGT POST.ID TIL AT GØRE GRUPPEN AF FOTOS UNIK, SOM DET OGSÅ SES NEDERST I JAVASCRIPT I GROUP -->
								</div>
							</div>
							
					</div>

		<% end %>
	
	
		<!-- <%# content_for :javascript do %>

		  <%# javascript_tag do %>

		     		TopUp.addPresets({
				    "#event_images_<%= event.id %> a": {
				      group: "event_images_<%= event.id %>",
					  title: "{alt} - {current} af {total}",
				      layout: "dashboard",
				      modal: 1,
				      resizable: 0,
				      readAltText: 1,
					  shaded: 1,
					  effect: 'switch'
					    },
				    ".shaded": {
				      shaded: 1
				    }
				  });


		    <%# end %>

		<%# end %> -->
	

	<% end %>

</div>








